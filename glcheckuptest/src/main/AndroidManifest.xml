<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ch.shibastudio.glcheckuptest">

    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_glcheckup"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_glcheckup"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".GLActivity"/>
        <activity android:name=".TextureActivity"/>
        <activity android:name=".TextureTestActivity"/>
    </application>

</manifest>